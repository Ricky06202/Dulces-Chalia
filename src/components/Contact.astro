---
// src/components/Contact.astro
const phone = '6982-3300';
const email = 'chaliaarce@gmail.com';
---
<section id="contact" class="bg-amber-100 py-16 md:py-20">
    <div class="container mx-auto px-4 max-w-xl text-center">
        <h2 class="text-4xl font-bold text-amber-900 mb-6">¡Contáctanos para tu Pedido!</h2>
        <p class="text-xl text-amber-800 mb-10">
            Diseñemos juntos el dulce perfecto para tu celebración.
        </p>

        <div class="space-y-6">
            <div class="flex items-center justify-center space-x-3">
                <span class="text-3xl text-green-600">📞</span> 
                <div>
                    <p class="text-sm font-semibold text-gray-600">Teléfono / WhatsApp</p>
                    <a href={`tel:${phone}`} class="text-2xl font-bold text-amber-900 hover:text-amber-700 transition-colors">
                        {phone}
                    </a>
                </div>
            </div>

            <div class="flex items-center justify-center space-x-3">
                <span class="text-3xl text-red-600">📧</span>
                <div>
                    <p class="text-sm font-semibold text-gray-600">Correo Electrónico</p>
                    <a href={`mailto:${email}`} class="text-2xl font-bold text-amber-900 hover:text-amber-700 transition-colors">
                        {email}
                    </a>
                </div>
            </div>
        </div>
        
        <p class="mt-10 text-lg italic text-amber-700">
            ¡Estaré encantada de endulzar tu próxima celebración!
        </p>
    </div>
</section>